# 使用 spring-ai-starter-mcp-server
spring:
  ai:
    mcp:
      server:
        name: yu-image-search-mcp-server
        version: 1.0.0
        stdio: true
        type: SYNC # 同步
  #stdio
  main:
    web-application-type: none
    banner-mode: off
